name: uctg
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  csv: ^6.0.0
  desktop_drop: ^0.4.4
  dotted_border: ^2.1.0
  filepicker_windows: ^2.1.0
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  path: ^1.9.0
  path_provider: ^2.1.2
  timetable_view: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
